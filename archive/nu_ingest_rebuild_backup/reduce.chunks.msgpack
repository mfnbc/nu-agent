–—ÙNarchive/external_nushell_repo_backup/nushell.github.io/commands/docs/reduce.mdÙ@ca978b1b40b34d8d83e232c0865cae103c25122fa5ee00aad86a17c8a10921a1­title: reduce‘§filtersÙ<








``reduce``
[link: /commands/categories/filters.md] ’ÙhTitle: title: reduce
Path: filters
Content:









``reduce``
[link: /commands/categories/filters.md] —ÙNarchive/external_nushell_repo_backup/nushell.github.io/commands/docs/reduce.mdÙ@d37fb724ab5be38ed3e2eb8ca46f6a52ec6255e1ac0f973ba4b3927fe49f8fd9­title: reduce‘©Signature¿``> reduce {flags} (closure)``
’ÙMTitle: title: reduce
Path: Signature
Content:
``> reduce {flags} (closure)``
—ÙNarchive/external_nushell_repo_backup/nushell.github.io/commands/docs/reduce.mdÙ@d6e9a80a531f0be86e9b0133cabdc08f60fdd238c04ce877869f6d5a10e891a6­title: reduce‘¥FlagsÙ2``--fold, -f {any}``
: Reduce with initial value.
’Ù\Title: title: reduce
Path: Flags
Content:
``--fold, -f {any}``
: Reduce with initial value.
—ÙNarchive/external_nushell_repo_backup/nushell.github.io/commands/docs/reduce.mdÙ@9e5bdc8fabdd228cbb36ff03c49d4af8ded6c06bcb980b3ec03e75858136670f­title: reduce‘ªParametersÙ!``closure``
: Reducing function.
’ÙPTitle: title: reduce
Path: Parameters
Content:
``closure``
: Reducing function.
—ÙNarchive/external_nushell_repo_backup/nushell.github.io/commands/docs/reduce.mdÙ@fde4cad5cc350201b6836fb1bd493dcfc144c2256c63d94e838c9eee50609724­title: reduce‘³Input/output types:Ù3input
output
list
<
any
>

any
table
any
range
any
’ÙkTitle: title: reduce
Path: Input/output types:
Content:
input
output
list
<
any
>

any
table
any
range
any
—ÙNarchive/external_nushell_repo_backup/nushell.github.io/commands/docs/reduce.mdÙ@e0ec6b4042fe566e19f1d51d49ba1bc4976cccd391de03494d54df154c86bf2d­title: reduce‘¨ExamplesÚSum values of a list (same as 'math sum').
```Fenced(Borrowed("nu"))
> [ 1 2 3 4 ] | reduce {|it, acc| $it + $acc }
10

```
``reduce``
 accumulates value from left to right, equivalent to (((1 - 2) - 3) - 4).
```Fenced(Borrowed("nu"))
> [ 1 2 3 4 ] | reduce {|it, acc| $acc - $it }
-8

```
Sum values of a list, plus their indexes.
```Fenced(Borrowed("nu"))
> [ 8 7 6 ] | enumerate | reduce --fold 0 {|it, acc| $acc + $it.item + $it.index }
24

```
Sum values with a starting value (fold).
```Fenced(Borrowed("nu"))
> [ 1 2 3 4 ] | reduce --fold 10 {|it, acc| $acc + $it }
20

```
Iteratively perform string replace (from left to right): 'foobar' -> 'bazbar' -> 'quuxbar'.
```Fenced(Borrowed("nu"))
> [[foo baz] [baz quux]] | reduce --fold "foobar" {|it, acc| $acc | str replace $it.0 $it.1}
quuxbar

```
Replace selected characters in a string with 'X'.
```Fenced(Borrowed("nu"))
> [ i o t ] | reduce --fold "Arthur, King of the Britons" {|it, acc| $acc | str replace --all $it "X" }
ArXhur, KXng Xf Xhe BrXXXns

```
Add ascending numbers to each of the filenames, and join with semicolons.
```Fenced(Borrowed("nu"))
> ['foo.gz', 'bar.gz', 'baz.gz'] | enumerate | reduce --fold '' {|str all| $"($all)(if $str.index != 0 {'; '})($str.index + 1)-($str.item)" }
1-foo.gz; 2-bar.gz; 3-baz.gz

```
Concatenate a string with itself, using a range to determine the number of times.
```Fenced(Borrowed("nu"))
> let s = "Str"; 0..2 | reduce --fold '' {|it, acc| $acc + $s}
StrStrStr

```
Merge multiple records together, making use of the fact that the accumulated value is also supplied as pipeline input to the closure.
```Fenced(Borrowed("nu"))
> [{a: 1} {b: 2} {c: 3}] | reduce {|it| merge $it}
â•­â”€â”€â”€â”¬â”€â”€â”€â•®
â”‚ a â”‚ 1 â”‚
â”‚ b â”‚ 2 â”‚
â”‚ c â”‚ 3 â”‚
â•°â”€â”€â”€â”´â”€â”€â”€â•¯

```
’Ú5Title: title: reduce
Path: Examples
Content:
Sum values of a list (same as 'math sum').
```Fenced(Borrowed("nu"))
> [ 1 2 3 4 ] | reduce {|it, acc| $it + $acc }
10

```
``reduce``
 accumulates value from left to right, equivalent to (((1 - 2) - 3) - 4).
```Fenced(Borrowed("nu"))
> [ 1 2 3 4 ] | reduce {|it, acc| $acc - $it }
-8

```
Sum values of a list, plus their indexes.
```Fenced(Borrowed("nu"))
> [ 8 7 6 ] | enumerate | reduce --fold 0 {|it, acc| $acc + $it.item + $it.index }
24

```
Sum values with a starting value (fold).
```Fenced(Borrowed("nu"))
> [ 1 2 3 4 ] | reduce --fold 10 {|it, acc| $acc + $it }
20

```
Iteratively perform string replace (from left to right): 'foobar' -> 'bazbar' -> 'quuxbar'.
```Fenced(Borrowed("nu"))
> [[foo baz] [baz quux]] | reduce --fold "foobar" {|it, acc| $acc | str replace $it.0 $it.1}
quuxbar

```
Replace selected characters in a string with 'X'.
```Fenced(Borrowed("nu"))
> [ i o t ] | reduce --fold "Arthur, King of the Britons" {|it, acc| $acc | str replace --all $it "X" }
ArXhur, KXng Xf Xhe BrXXXns

```
Add ascending numbers to each of the filenames, and join with semicolons.
```Fenced(Borrowed("nu"))
> ['foo.gz', 'bar.gz', 'baz.gz'] | enumerate | reduce --fold '' {|str all| $"($all)(if $str.index != 0 {'; '})($str.index + 1)-($str.item)" }
1-foo.gz; 2-bar.gz; 3-baz.gz

```
Concatenate a string with itself, using a range to determine the number of times.
```Fenced(Borrowed("nu"))
> let s = "Str"; 0..2 | reduce --fold '' {|it, acc| $acc + $s}
StrStrStr

```
Merge multiple records together, making use of the fact that the accumulated value is also supplied as pipeline input to the closure.
```Fenced(Borrowed("nu"))
> [{a: 1} {b: 2} {c: 3}] | reduce {|it| merge $it}
â•­â”€â”€â”€â”¬â”€â”€â”€â•®
â”‚ a â”‚ 1 â”‚
â”‚ b â”‚ 2 â”‚
â”‚ c â”‚ 3 â”‚
â•°â”€â”€â”€â”´â”€â”€â”€â•¯

```
