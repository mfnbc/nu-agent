[package]
name = "nu_agent_common"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
blake3 = "1"
# reqwest and tempfile removed: not used directly in this crate
rmp-serde = "1"
