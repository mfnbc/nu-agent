# Rust build artifacts
/target/
/**/target/
crates/**/.cache/

# Editors
.idea/
.vscode/

# Local junk
*.log
*.tmp
.DS_Store

# External corpora and runtime artifacts
# (user runs `git clone <docs>`, `cargo build`, and the ingest pipeline to populate these)
external/
data/
tokenizers/
